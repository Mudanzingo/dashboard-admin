# Variables de entorno para la app web (Vite + Amplify)
# Copia este archivo a `.env` y completa los valores.
# NOTA: Vite solo expone variables que comienzan con `VITE_`.

# --- AWS Amplify / Cognito ---
VITE_AMPLIFY_REGION=us-east-1
VITE_AMPLIFY_USER_POOL_ID=us-east-1_XXXXXXXXX
VITE_AMPLIFY_USER_POOL_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX
# Dominio de Hosted UI (SIN protocolo). Formato: <subdominio>.auth.<region>.amazoncognito.com
# Ej.: myappdomain.auth.us-east-1.amazoncognito.com
VITE_AMPLIFY_COGNITO_DOMAIN=myappdomain.auth.us-east-1.amazoncognito.com
# Scopes de OAuth separados por comas
VITE_AMPLIFY_OAUTH_SCOPES=openid,profile,email
# Redirects permitidos por Cognito. Puedes poner múltiples, separados por coma.
# Asegúrate de agregarlos también en Allowed Callback URLs de Cognito.
VITE_AMPLIFY_REDIRECT_SIGN_IN=http://localhost:5173,http://localhost:5173/callback
VITE_AMPLIFY_REDIRECT_SIGN_OUT=http://localhost:5173,http://localhost:5173/login

# --- API Backend ---
# URL base de tu API (no el front). Ej.: http://localhost:3000 o https://api.tudominio.com
VITE_API_BASE_URL=http://localhost:3000
